# CORPUS: model configurations G2.2's ceiling is claimed over.
#
# Owner: the reviewer. Test DATA only -- nothing here imports from floatfea and
# nothing here is test code. One candidate per line, whitespace-separated
# key=value fields. Written at the sixth verdict on F2 step 4 (HEAD 15bbc3b).
#
# WHY THIS FILE EXISTS (BE3). The shipped gate poses ONE geometry in TWO
# orientations at THREE length units. Every negative control on it was designed
# by the same hand as the gate, so the coverage it demonstrates is coverage of
# the cases its author thought of. These are cases the author did not run. They
# are written down rather than left in a verdict so the next round can measure
# against them instead of re-deriving them.
#
# WHAT A PASSING SCAN MEANS. For each entry, the six constant-strain states are
# solved in the named orientation and the worst relative field error is compared
# against PATCH_TEST_EXACTNESS, and the worst recovered-resultant error against
# RESULTANT_EXACTNESS. A passing scan means every entry marked expect=hold is
# below both ceilings AND the 1e-6 single-element negative control still exceeds
# both counters at that entry. It does NOT mean the shipped test suite runs the
# entry -- the `runs_in_suite` field records that separately, and it is the
# coverage measurement.
#
# expect=hold    the ceilings are claimed to hold here
# expect=breach  measured above PATCH_TEST_EXACTNESS at this reviewer's run
# expect=raise   the configuration must RAISE, not default to a silent fallback
#
# FIELDS
#   id            stable name
#   section       circular_tube D=<m> t=<m>            (the only shape basis.kappa knows)
#   stations      total member length in m; the mesh is the gate's own ratio set
#   orient        axis | skew | vertical | in_plane_y
#   extra         orientation_node=..., roll=<rad>, I_y_over_I_z=<r>, or none
#   runs_in_suite yes | no       -- does a shipped test execute this configuration
#   expect        hold | breach
#
# ---------------------------------------------------------------------------
id=posed_metre        section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew        extra=none                       runs_in_suite=yes expect=hold
id=posed_axis         section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=axis        extra=none                       runs_in_suite=yes expect=hold

# --- orientations the gate never poses -------------------------------------
# The platform's spars are vertical. A member along global Z is the branch the
# default local-axis construction must refuse; with an orientation_node supplied
# it is the branch F3's real model will take, and no rung-1 test enters it.
id=vertical_no_onode  section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=vertical    extra=none                       runs_in_suite=no  expect=raise
id=vertical_onode     section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=vertical    extra=orientation_node=5,0,0     runs_in_suite=no  expect=hold
id=in_plane_y         section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=in_plane_y  extra=none                       runs_in_suite=no  expect=hold
id=roll_quarter       section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew        extra=roll=0.7853981634          runs_in_suite=no  expect=hold
id=roll_one_rad       section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew        extra=roll=1.0                   runs_in_suite=no  expect=hold

# --- the stubby member ------------------------------------------------------
# R15 recorded that on a stubby configuration the six responses to the 1e-6
# control fell BELOW PATCH_TEST_EXACTNESS_COUNTER. That was measured under the
# pre-R2 mixed-unit error measure. Kept as an entry so the claim stays under
# measurement rather than being withdrawn by argument.
id=stubby_thin        section=circular_tube,D=2.000,t=0.04000 stations=3.00 orient=skew        extra=none                       runs_in_suite=no  expect=hold
id=stubby_thick       section=circular_tube,D=2.000,t=0.10000 stations=3.00 orient=skew        extra=none                       runs_in_suite=no  expect=hold

# --- slenderness, at constant D/t = 50 --------------------------------------
# tolerances.py declares a boundary at element L/r = 119 and F2.md D7 item 5
# turns it into an F3 obligation. These entries are the counter-evidence: the
# error is NOT monotone in slenderness and there are breaches well inside the
# declared-safe region. Any F3 builder assertion of the form "L/r < 119" admits
# slender_L_r_79.
id=slender_L_r_47     section=circular_tube,D=0.200,t=0.00400 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=hold
id=slender_L_r_63     section=circular_tube,D=0.150,t=0.00300 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=hold
id=slender_L_r_79     section=circular_tube,D=0.120,t=0.00240 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=breach
id=slender_L_r_86     section=circular_tube,D=0.110,t=0.00220 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=hold
id=slender_L_r_94     section=circular_tube,D=0.100,t=0.00200 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=breach
id=slender_L_r_99     section=circular_tube,D=0.095,t=0.00190 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=hold
id=slender_L_r_118    section=circular_tube,D=0.080,t=0.00160 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=breach

# --- the section the type cannot build --------------------------------------
# basis.kappa knows only thin_tube and solid_circular, both of which force
# I_y == I_z, so no test in the repository can distinguish the two bending
# inertias. Measured: replacing every `section.I_y` in floatfea/element/beam.py
# with `section.I_z` leaves all 366 tests green. Kept as an entry so the hole is
# scored rather than argued; it opens when a non-circular shape exists.
id=aniso_I_y_2x       section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew        extra=I_y_over_I_z=2.0           runs_in_suite=no  expect=hold

# ===========================================================================
# ADDED AT THE SEVENTH VERDICT (HEAD 83b2de2). 19 entries, none of them seen
# by the implementer, none of them a variation on an entry above that a reader
# of the file could have anticipated. Every figure below was measured by the
# reviewer through the shipped `test_corpus_configurations` helpers at 83b2de2.
#
# TWO of these nineteen are expected to be RED at 83b2de2, and the redness is
# the finding rather than a mistake in the entry. They are marked <-- RED and
# named in the verdict (R58, R59). The other seventeen pass.
# ---------------------------------------------------------------------------

# --- the UNIT axis, which the corpus had none of ---------------------------
# The gate is parametrised over three length units; the corpus was not. These
# are the posed geometry scaled by 1000 and by 1/1000 (a real 600 m tube and a
# real 0.6 mm one, SI material both times), which is the cheapest way to reach
# a very different cond(K_ff) through the fields this schema already has.
# Measured worst field error: 7.0797e-14 and 2.6751e-13, against a floor-aware
# ceiling of 3.5962e-08 and 5.3140e-07 -- five and six orders of slack, where
# the same form has 65x at the posed geometry. Both hold against the CONSTANT.
id=unit_km_similar    section=circular_tube,D=600.0,t=12.00000 stations=9670.0 orient=skew    extra=none                       runs_in_suite=no  expect=hold
id=unit_mm_similar    section=circular_tube,D=0.0006,t=0.000012 stations=0.00967 orient=skew   extra=none                       runs_in_suite=no  expect=hold

# --- wall thickness, held out of every sweep so far -------------------------
# Every slenderness entry above holds D/t = 50. These move D/t alone.
id=thick_wall_D_t_5   section=circular_tube,D=0.600,t=0.12000 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=hold
id=thin_wall_D_t_200  section=circular_tube,D=0.600,t=0.00300 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=hold

# --- past both ends of the slenderness sweep --------------------------------
# 189 is a FOURTH measured breach of the constant (4.4147e-12), well outside
# the L/r = 15.7 .. 117.9 range the floor-aware factor was fitted over, and it
# stays under the floor-aware ceiling (1.0737e-10). The three between 104 and
# 126 fill the gaps the existing samples leave, and all three sit at 0.80-0.90
# of the constant -- the scatter straddling 1 rather than crossing it.
id=slender_L_r_105    section=circular_tube,D=0.090,t=0.00180 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=hold
id=slender_L_r_111    section=circular_tube,D=0.085,t=0.00170 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=hold
id=slender_L_r_126    section=circular_tube,D=0.075,t=0.00150 stations=9.67 orient=skew        extra=none                       runs_in_suite=no  expect=hold
id=very_slender_L_r_189 section=circular_tube,D=0.050,t=0.00100 stations=9.67 orient=skew      extra=none                       runs_in_suite=no  expect=breach

# --- the stubby end, where R15's withdrawn claim comes back ----------------
# THE SIXTH VERDICT WITHDREW R15 ON MEASUREMENT AND THE WITHDRAWAL WAS
# PREMATURE. It was withdrawn on D = 2.0 m at L = 3.0 m, where the six
# responses to the 1e-6 single-element control are 1.08e-07 .. 1.44e-07, above
# PATCH_TEST_EXACTNESS_COUNTER = 1.0e-7. At the SAME section and L = 1.0 m the
# weakest response is 9.5011e-08 -- BELOW the counter. The counter is a
# property of the posed mesh and section, exactly as R15 said, and its entry
# still does not say so.
# This entry is expected to redden `test_the_corpus_entry_still_DETECTS_a_defect`.
id=very_stubby_L_r_0p5 section=circular_tube,D=2.000,t=0.04000 stations=1.00 orient=skew       extra=none                       runs_in_suite=no  expect=hold
id=short_member_L_r_10 section=circular_tube,D=0.100,t=0.00200 stations=1.00 orient=skew       extra=none                       runs_in_suite=no  expect=hold

# --- the orientation guard, at its own boundary -----------------------------
# MEMBER_ORIENTATION_DEGENERACY = 0.05. An orientation node ON the member axis
# and one 0.003 deg off it must both raise; one 0.0501 rad off it must not, and
# must still be exact. Measured: raise, raise, and 3.8831e-15 with the 1e-6
# control at 1.0764e-07. The guard is sharp at its declared value.
id=onode_collinear    section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=axis        extra=orientation_node=20,0,0    runs_in_suite=no  expect=raise
id=onode_0p003deg_off section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=axis        extra=orientation_node=20,0.000999,0 runs_in_suite=no expect=raise
id=onode_just_outside section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=axis        extra=orientation_node=20,1.002839,0 runs_in_suite=no expect=hold

# --- roll, past the values already recorded ---------------------------------
id=roll_half_pi       section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew        extra=roll=1.5707963268          runs_in_suite=no  expect=hold
id=roll_negative      section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew        extra=roll=-2.5                  runs_in_suite=no  expect=hold

# --- vertical, with the orientation node on the OTHER transverse axis -------
id=vertical_onode_y   section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=vertical    extra=orientation_node=0,5,0     runs_in_suite=no  expect=hold

# --- the I_y / I_z index, from the other side and further out ---------------
# aniso_I_y_2x above catches the beam.py:73 mutation. These check the pin is a
# ratio and not a threshold: I_y BELOW I_z, and an order of magnitude above.
id=aniso_I_y_half     section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew        extra=I_y_over_I_z=0.5           runs_in_suite=no  expect=hold
id=aniso_I_y_10x      section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew        extra=I_y_over_I_z=10.0          runs_in_suite=no  expect=hold

# --- the corpus INTERFACE itself, which nothing has ever tested -------------
# test_corpus_configurations.py:26-28 states: "An entry naming a field this
# module cannot build is a FAILURE, not a skip: a corpus entry that silently
# does nothing is the vacuous-parameter failure AM5 named." Measured at
# 83b2de2, it is a skip. `extra=roll_rad=1.0` -- a plausible typo for `roll=`,
# and the expected failure mode when the corpus and the runner have different
# authors -- falls off the end of the if/elif chain in `_build` and produces a
# result BIT-IDENTICAL to `extra=none` (1.251313e-14 both times). So does
# `extra=nonsense=3.0`, and so does an unrecognised top-level field.
# This entry asserts the module's own docstring. It is expected to redden until
# `_build` raises on a field it does not honour.
id=typo_field_roll_rad section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew       extra=roll_rad=1.0               runs_in_suite=no  expect=raise

# ===========================================================================
# ADDED AT THE EIGHTH VERDICT (HEAD ad2246a). 14 entries, none seen by the
# implementer. Every figure below was measured by the reviewer through the
# shipped `test_corpus_configurations` helpers at ad2246a.
#
# FIVE of these fourteen are expected to be RED at ad2246a, and the redness is
# the finding rather than a mistake in the entry. They are marked <-- RED.
# ---------------------------------------------------------------------------

# --- the floor-aware ceiling REJECTS A CLEAN MODEL --------------------------
# The unit axis and the roll axis are each already in this file; nothing had
# combined them with a wall thickness. Measured at ad2246a on the first entry:
#   worst field error 4.4681e-13  (axial state, skew)
#   floor-aware ceiling 5.0 * cond(K~) * eps = 4.2034e-13   -> err/ceiling 1.063
#   PATCH_TEST_EXACTNESS = 1e-12                            -> err/constant 0.447
#   L/D = 16.12 (admissible), 1e-6 control fires at 1.0763e-07
# A clean, admissible model that the CONSTANT accepts with 2.2x of margin and
# the floor-aware term REFUSES. It is round-off and nothing else: the same
# configuration gives err/ceiling 0.174 under NATURAL, 0.112 under MMD_ATA and
# 0.195 under MMD_AT_PLUS_A -- a 6.1x permutation spread on a change that
# provably cannot move the exact solution. The other two entries are the same
# corner without the breach (0.529 and 0.536 of their ceilings).
id=unit_mm_D_t_100_roll_neg section=circular_tube,D=0.000600,t=0.00000600 stations=0.00967 orient=skew extra=roll=-2.5 runs_in_suite=no expect=hold
id=unit_mm_D_t_200_roll_1 section=circular_tube,D=0.000600,t=0.00000300 stations=0.00967 orient=skew extra=roll=1.0 runs_in_suite=no expect=hold
id=unit_mm_roll_half_pi section=circular_tube,D=0.000600,t=0.00001200 stations=0.00967 orient=skew extra=roll=1.5707963268 runs_in_suite=no expect=hold

# --- the FRAME axis, which cond(K~) is not invariant to ---------------------
# cond(K_ff) is identical for every orientation of the same beam (9.20958e+02,
# exactly, because a rotation is an orthogonal congruence). cond(K~) is NOT:
# 1.0596e+02 axis-aligned, 3.8491e+02 skew, 6.3199e+02 at 45 deg -- 6.0x on the
# same physical model. And along the axis-aligned orientation cond(K~) barely
# tracks slenderness at all: 105.96 at D = 0.6 to 433.88 at D = 0.05, 4.1x,
# against the 121.7x the tolerance entry publishes for the skew orientation.
# These three entries put the other orientation under measurement at the slender
# end. All three hold; the ceiling there is 4.6e-13 .. 4.8e-13, i.e. TIGHTER
# than the constant and flat, which is the shape of a constant, not of a floor.
id=slender_axis_L_r_189 section=circular_tube,D=0.050,t=0.00100 stations=9.67 orient=axis extra=none runs_in_suite=no expect=hold
id=slender_axis_L_r_118 section=circular_tube,D=0.080,t=0.00160 stations=9.67 orient=axis extra=none runs_in_suite=no expect=hold
id=slender_in_plane_y_L_r_94 section=circular_tube,D=0.100,t=0.00200 stations=9.67 orient=in_plane_y extra=none runs_in_suite=no expect=hold

# --- the parser is strict about FIELD NAMES and not about FIELD VALUES ------
# BH3 closed the top-level key and the `extra=` prefix. The `section=` value is
# still unparsed: `_section` reads only the D and t after the first comma and
# never looks at the shape token or at any further key. Measured at ad2246a,
# all three produce 1.251313e-14 -- BIT-IDENTICAL to the reference entry:
#   section=rectangle,D=0.600,t=0.01200          builds a CIRCULAR TUBE
#   section=circular_tube,D=0.600,t=0.01200,b=0.300   the b is dropped
#   a repeated `orient=` field                   silently takes the last one
# The first is the one that matters: this file's own header says circular_tube
# is "the only shape basis.kappa knows", so the natural way for the next
# reviewer to write the non-circular candidate the aniso block is waiting for is
# `section=rectangle,...` -- and it would be scored green as a circular tube.
id=typo_section_shape section=rectangle,D=0.600,t=0.01200 stations=9.67 orient=skew extra=none runs_in_suite=no expect=raise
id=typo_section_extra_key section=circular_tube,D=0.600,t=0.01200,b=0.300 stations=9.67 orient=skew extra=none runs_in_suite=no expect=raise
id=duplicate_orient_field section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew extra=none runs_in_suite=no expect=raise orient=axis

# --- the beam admission limit, at its own boundary and just inside it -------
# BEAM_ADMISSION_L_OVER_D = 2.0, on the member. These pin both sides of it: the
# first two agree with the limit as coded. The third is the adjudication
# evidence: at L/D = 1.3 the 1e-6 negative control responds at 1.0853e-07,
# ABOVE PATCH_TEST_EXACTNESS_COUNTER = 1.0e-7, so the gate demonstrably still
# fires there and the member is refused anyway. Reproduced at ad2246a with the
# limit bypassed: the control fails only below L/D ~ 1.2
# (0.50 -> 9.5011e-08, 1.00 -> 8.8011e-08, 1.20 -> 1.0758e-07, 1.50 -> 1.0839e-07).
id=admissible_L_over_D_2p05 section=circular_tube,D=2.000,t=0.04000 stations=4.10 orient=skew extra=none runs_in_suite=no expect=hold
id=inadmissible_L_over_D_1p95 section=circular_tube,D=2.000,t=0.04000 stations=3.90 orient=skew extra=none runs_in_suite=no expect=raise
id=stubby_L_over_D_1p3 section=circular_tube,D=2.000,t=0.04000 stations=2.60 orient=skew extra=none runs_in_suite=no expect=hold

# --- section physics the type permits and nothing checks --------------------
# `Section.circular_tube` labels EVERY wall thickness `thin_tube` and therefore
# draws Cowper's THIN-WALL-LIMIT coefficient 0.53061 whatever the wall is. The
# hollow-circle coefficient (Cowper Table 1, verified against this repository's
# own two endpoints: it returns 0.53061 as Ri/Ro -> 1 and 0.88636 at Ri/Ro = 0)
# gives 0.53297 at D/t = 20, 0.58238 at D/t = 5 (-8.9%), and 0.88366 at the
# constructor's own limit D/t -> 2.07 (-40%). `thick_wall_D_t_5` above is
# already inside that. This entry sits at D/t = 2.1 and passes G2.2, which is
# the point: G2.2 is documented blind to a uniform kappa error, so nothing in
# rung 1 can see a 40% wrong shear coefficient.
id=nearly_solid_D_t_2p1 section=circular_tube,D=0.600,t=0.28571 stations=9.67 orient=skew extra=none runs_in_suite=no expect=hold

# --- PATCH_TEST_EXACTNESS_COUNTER's domain fails on a SECOND axis -----------
# R58 asked for the counter's operating point. The answer given was a limit on
# stubbiness. Anisotropy is a second axis and the admission limit does not cover
# it: at I_y/I_z = 500 the member is L/D = 16.12, comfortably admissible, and
# the weakest state responds to the 1e-6 control at 8.6804e-08 -- BELOW the
# counter. Non-monotone (1000x gives 9.4675e-08), which is the signature of the
# same round-off. The ratio is extreme and no real section reaches it; the entry
# is evidence about the counter's undeclared domain, not about the element.
id=aniso_I_y_500x section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew extra=I_y_over_I_z=500.0 runs_in_suite=no expect=hold

# ===========================================================================
# NINTH VERDICT (F2 step 4). The axes this round created: the two-tier scheme,
# the validated-domain boundary at member lambda = 60, and the section-SIZE
# axis that the envelope behind that boundary does not span.
# ===========================================================================

# --- inside the validated domain, on the axis its envelope does not span ----
# `G22_VALIDATED_MEMBER_LAMBDA = 60` was set from an envelope over 6 angles x 7
# rolls x 6 states AT ONE SECTION, and its entry claims "10x of margin over that
# envelope". Section SIZE is a free axis: these two sit at member lambda 56.0 and
# 58.3, both admissible (L/D 19.8 and 20.6), both with the 1e-6 control firing,
# and they reach 0.532 and 0.356 of PATCH_TEST_EXACTNESS. The claimed margin is
# 10x; the measured margin at the boundary, inside this schema, is 1.9x.
#
# NOT EXPRESSIBLE IN THIS SCHEMA, and it is the decisive case: with the member
# direction free rather than one of the four named orientations,
#   D = 5.6809e-04 m, t = 5.24165e-06 m, L = 1.159406e-02 m,
#   axis = (-0.384196, -0.923213, 0.008385)/|.|, roll = -1.5774 rad
# is clean, admissible (L/D = 20.41), lambda = 58.26 (INSIDE the domain), the
# control fires at 1.0766e-07 -- and the worst state is 7.5786e-12, 7.58x
# PATCH_TEST_EXACTNESS. The permutation cell puts it at round-off: COLAMD
# 7.58e-12, NATURAL 4.36e-12, MMD_ATA 7.27e-14, MMD_AT_PLUS_A 1.11e-13, a 104x
# spread on a quantity the ordering cannot change. `orient=` needs a free vector
# before this file can carry it.
id=boundary_mm_lam56_roll section=circular_tube,D=0.000362767,t=9.50772e-07 stations=0.00716362 orient=skew extra=roll=1.4181 runs_in_suite=no expect=hold
id=boundary_mm_lam58_skew section=circular_tube,D=0.000362767,t=9.50772e-07 stations=0.0074581 orient=skew extra=roll=2.89167 runs_in_suite=no expect=hold

# --- the tier boundary is a 20x step in the ceiling, not in the error --------
# One section, one orientation, one roll; only the member length moves, from
# member lambda 59 to 61. The measured error moves 2.3x. The ceiling applied to
# it moves 20x, from PATCH_TEST_EXACTNESS to PATCH_TEST_ROUNDOFF. The third
# entry is the same lambda = 61 member turned axis-aligned, where the error is
# 1.2e-14 and the ceiling it is now judged against is 2e-11: 1650x of headroom
# on an entry that cleared the constant by 83x.
id=straddle_below_lam59 section=circular_tube,D=0.000600,t=6.0e-06 stations=0.0123913 orient=skew extra=roll=-2.5 runs_in_suite=no expect=hold
id=straddle_above_lam61 section=circular_tube,D=0.000600,t=6.0e-06 stations=0.0128113 orient=skew extra=roll=-2.5 runs_in_suite=no expect=hold
id=straddle_above_lam61_axis section=circular_tube,D=0.000600,t=6.0e-06 stations=0.0128113 orient=axis extra=roll=-2.5 runs_in_suite=no expect=hold

# --- tier 2 applied where nothing asked for it -------------------------------
# Both are past the boundary on member lambda and both are in orientations whose
# measured exponent on lambda is 0.68 (the implementer's fit) / 0.52 (mine),
# against ~2.2 in skew. They measure 8.1e-15 and 7.5e-15 -- 120x and 130x BELOW
# PATCH_TEST_EXACTNESS -- and the shipped runner judges them against 2e-11. The
# cost is measurable: on `slender_axis_L_r_189` a single-element relative
# stiffness defect of 1.73e-10 passes the round-off tier, where the constant
# catches anything above 8.65e-12.
id=tier2_axis_lam65_metre section=circular_tube,D=0.600,t=0.01200 stations=13.5156 orient=axis extra=none runs_in_suite=no expect=hold
id=tier2_in_plane_y_lam70 section=circular_tube,D=0.600,t=0.01200 stations=14.5553 orient=in_plane_y extra=none runs_in_suite=no expect=hold

# --- member_lambda reads I_z alone, so the weak axis is invisible to the tier -
# `member_lambda` (admissibility.py:95) is `L / sqrt(I_z/A)`. For I_y < I_z the
# slenderness about the WEAK axis is larger and the tier never sees it. The first
# entry has I_y/I_z = 0.05: lambda on I_z is 46.5 and on the weak axis 208, and
# it is placed in the EXACTNESS tier. The second is at I_y/I_z = 0.5, lambda 59
# and weak-axis lambda 83.4 -- also exactness. Both hold today, which is the
# point: the collection the tier inspects cannot contain the weak-axis case, so
# a pass here certifies nothing about it.
id=aniso_I_y_twentieth section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew extra=I_y_over_I_z=0.05 runs_in_suite=no expect=hold
id=aniso_I_y_half_lam59 section=circular_tube,D=0.600,t=0.01200 stations=12.268 orient=skew extra=I_y_over_I_z=0.5 runs_in_suite=no expect=hold

# --- R79's kappa axis, now inside the validated domain -----------------------
# D/t = 3, where Cowper's hollow-circle coefficient is 0.71402 against the
# shipped 0.53061 -- 25.7% wrong -- at member lambda 50, comfortably inside the
# domain the exactness claim now names. It passes, because G2.2 is documented
# blind to a uniform kappa error. The entry records that the validated domain
# says nothing about kappa.
id=thick_wall_D_t_3_lam50 section=circular_tube,D=0.600,t=0.20000 stations=7.90569 orient=skew extra=none runs_in_suite=no expect=hold

# --- the parser's remaining unvalidated VALUES (R75 one field over) ----------
# R75 closed the section spec's SHAPE. Two further values are still unchecked.
# The first passes: `section=D=0.6,...` is refused by the new shape check, at
# PARSE time, which is the only place a refusal is safe here (see the note at
# the end of this block).
id=section_shape_absent section=D=0.6,t=0.012 stations=9.67 orient=skew extra=none runs_in_suite=no expect=raise
# THE THIRD IS RED, and it is not a corpus problem. `extra=roll=NaN` parses,
# BUILDS A MODEL SILENTLY, and surfaces only inside the solve as
# `RuntimeError: Factor is exactly singular` -- a message that names a mechanism
# where the cause is a bad input field. Measured in floatfea, not in this
# module: `rotation_matrix(..., roll_rad=nan)` RETURNS a matrix containing NaN
# with a numpy RuntimeWarning and no refusal (`local_axes.py:118`), and so does
# roll_rad=inf. The degeneracy guard twenty lines above refuses a near-parallel
# member loudly; a NaN roll goes through.
id=roll_field_nan section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew extra=roll=NaN runs_in_suite=no expect=raise

# TWO ENTRIES ARE NOT COMMITTED, because each takes the whole module out at
# COLLECTION
# rather than producing a failing test, and a corpus that cannot be run is not a
# corpus. `INADMISSIBLE = [...]` (test_corpus_configurations.py:352) runs
# `_inadmissible` at IMPORT, which calls `_section` and `member_l_over_d`
# unguarded. Anything either of them refuses is an import-time crash, so an
# `expect=raise` entry can only be carried here when the refusal happens at
# PARSE time. The two lines, for whoever fixes it -- each gives
# `ERROR ... Interrupted: 1 error during collection`:
#   id=section_negative_wall section=circular_tube,D=0.600,t=-0.01200 stations=9.67 orient=skew extra=none runs_in_suite=no expect=raise
#   id=stations_negative     section=circular_tube,D=0.600,t=0.01200 stations=-9.67 orient=skew extra=none runs_in_suite=no expect=raise

# ===========================================================================
# TENTH ROUND (HEAD 47d117e). The gate changed QUANTITY: it now asserts the
# interior out-of-balance of the exact field, `max|K_hat w|_interior /
# (max|K_hat| max|w|)`, with no solve in it. Every entry below was scored
# against that quantity by the reviewer before being written down.
#
# WHAT A PASSING SCAN MEANS FOR THESE ENTRIES. `expect=hold` now means two
# things and both are asserted: the six states' out-of-balance is below
# PATCH_TEST_EXACTNESS, AND the 1e-6 single-element control still exceeds
# PATCH_TEST_EXACTNESS_COUNTER. The second is the one this round is about.
# The header's older paragraph, which describes the solved field error against
# two ceilings, belongs to the retired quantity; it is left as the record of
# what the earlier entries were written against.
#
# THERE IS NO `expect` VALUE FOR "THE CEILING HOLDS AND THE GATE CANNOT FAIL
# HERE", which is the failure mode the new quantity introduces. Entries 2 and 3
# below are that shape and are written `expect=hold`, so the red comes from the
# detection assertion, which is where it belongs.
# ---------------------------------------------------------------------------

# --- the domain, moved from the ceiling to the counter -----------------------
# The 1e-6 defect's residual falls as 1/lambda^2, so detection -- not the
# ceiling -- is what runs out with slenderness. Measured by the reviewer at this
# HEAD, skew, the gate's own five-element station ratios:
#
#   member lam    558 (corpus worst)   600     620     650     900    1600
#   x counter            1.075x       1.088x  1.019x  0.927x  0.484x  0.153x
#                                              ^ pin   ^ first red
#
# So the corpus's most slender entry sits 7.5% from a red and 11% of member
# lambda from one. That boundary is recorded in no file in this repository, and
# `F2.md` sec. 5b Q6 says the domain question is "dissolved" and there is "no
# boundary". The ceiling's domain is genuinely dissolved -- 1500 random
# configurations over D, D/t, member lambda, free direction and free roll put
# the worst clean value at 0.092x of 5e-15. The counter's is not.
id=lam620_counter_pin   section=circular_tube,D=0.600,t=0.01200 stations=128.9182   orient=skew                            runs_in_suite=no  expect=hold
id=lam900_skew_undetectable section=circular_tube,D=0.600,t=0.01200 stations=187.1394 orient=skew                          runs_in_suite=no  expect=hold
id=lam900_axis_undetectable section=circular_tube,D=0.600,t=0.01200 stations=187.1394 orient=axis                          runs_in_suite=no  expect=hold

# --- the ninth verdict's counterexample, now expressible ---------------------
# `orient=` takes a free direction as of e13a69f, so the configuration that
# produced the ninth verdict's STOP can be carried as an entry instead of as a
# comment. Under the retired quantity it breached 1e-12 by 7.58x at member
# lambda 58.26, admissible and unwarned. Under the shipped quantity the reviewer
# measures 0.022x of the ceiling and 113x of the counter. It is here so that a
# future change to the quantity is scored against the case that broke the last
# one.
id=r81_counterexample   section=circular_tube,D=5.6809e-4,t=5.24165e-6 stations=1.159406e-2 orient=-0.384196,-0.923213,0.008385 extra=roll=-1.5774 runs_in_suite=no expect=hold

# --- the degeneracy boundary, taken from both sides --------------------------
# MEMBER_ORIENTATION_DEGENERACY = 0.05 is a sine. These two straddle it by
# +2% and -2% of the threshold: 0.0501 is admitted, 0.0499 is refused. A
# threshold sampled on one side only is a threshold nobody has measured.
id=degeneracy_just_admitted section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=0.0501,0,0.99874                   runs_in_suite=no  expect=hold
id=degeneracy_just_refused  section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=0.0499,0,0.99875                   runs_in_suite=no  expect=raise

# --- the free direction reaches sections and axes the four names cannot ------
id=thickwall_free_dir   section=circular_tube,D=0.600,t=0.29000 stations=9.67       orient=0.11,-0.97,0.21                  runs_in_suite=no  expect=hold
id=aniso_free_dir_weak  section=circular_tube,D=0.600,t=0.01200 stations=9.67       orient=0.6,0.2,-0.77 extra=I_y_over_I_z=0.02 runs_in_suite=no expect=hold

# --- the parser's remaining value-shaped holes -------------------------------
# `orient=1e308,1e308,0`: the norm OVERFLOWS to inf, `v / inf` is the zero
# vector, and the `norm == 0.0` guard tests the input norm rather than the
# result -- so `_direction` returns the zero vector its own docstring says it
# refuses, with a numpy RuntimeWarning. It is refused downstream, by the node
# constructor, for a different reason. expect=raise passes today and passes for
# the wrong reason.
id=orient_norm_overflow section=circular_tube,D=0.600,t=0.01200 stations=9.67       orient=1e308,1e308,0                    runs_in_suite=no  expect=raise
id=orient_subnormal_near_z section=circular_tube,D=0.600,t=0.01200 stations=9.67    orient=1e-320,0,1                       runs_in_suite=no  expect=raise
# A finite roll of 1e300 carries no significant digits of angle. It builds, and
# the gate holds on it.
id=roll_1e300           section=circular_tube,D=0.600,t=0.01200 stations=9.67       orient=skew extra=roll=1e300            runs_in_suite=no  expect=hold
# ROLL AND ANISOTROPY TOGETHER cannot be written: `extra=` takes one key. This
# line is what the reviewer wants MEASURED, so it says expect=hold. The module
# cannot parse it, and `_parse` REPLACES the reviewer's `expect` with "raise"
# before the per-entry test compares `expect` against "raise" -- so this entry
# reads green while measuring nothing. That is the finding; the entry stays as
# it is until the module either builds it or reports the disagreement.
id=roll_and_aniso_together section=circular_tube,D=0.600,t=0.01200 stations=9.67    orient=skew extra=roll=0.3,I_y_over_I_z=0.5 runs_in_suite=no expect=hold

# --- eleventh round: the counter's domain is NOT on the reported lambda axis --
# Revision 11 proposes bounding the model at member lambda <= 300 (F6: 200 in
# compression) so the 1e-6 control still clears the counter everywhere admitted.
# These four entries measure that proposal on the coordinate it does not sample.
# `member_lambda(entry)` rebuilds the section from `section=` and discards
# `extra=I_y_over_I_z=`, so the lambda the runner PRINTS is the strong axis. The
# governing (min-I) lambda is in the comment beside each line, and it is what the
# 1/lambda^2 response actually follows.
#
# aniso_weak_lam154_undetectable: reported lambda 153.9 -- inside BOTH proposed
# limits -- governing lambda 1539.0, clean 0.044x the ceiling, and the 1e-6
# defect responds at 2.1370e-14 = 0.214x the counter. The gate holds here and
# CANNOT FAIL here, at half the proposed compression limit. A limit written on
# the reported lambda does not bound the counter; one written on min(I_y, I_z)
# would refuse this line at 1539.
id=aniso_weak_lam154_undetectable section=circular_tube,D=0.600,t=0.01200 stations=32.0 orient=skew extra=I_y_over_I_z=0.01  runs_in_suite=no expect=hold
# The same reported lambda, one twentieth instead of one hundredth: governing
# lambda 688.2, det 1.0684e-13 = 1.068x the counter. A 7% loss of sensitivity
# reddens it. Two entries with the SAME printed lambda, 5x apart in detection.
id=aniso_weak_lam154_pin        section=circular_tube,D=0.600,t=0.01200 stations=32.0 orient=skew extra=I_y_over_I_z=0.05  runs_in_suite=no expect=hold
# Governing lambda 458.1, det 2.3999e-13 = 2.400x. Brackets the pin from below on
# the anisotropy axis rather than on the length axis.
id=aniso_weak_lam102_detects    section=circular_tube,D=0.600,t=0.01200 stations=21.3 orient=skew extra=I_y_over_I_z=0.05  runs_in_suite=no expect=hold
# Anisotropy the OTHER way, same length: I_y/I_z = 100 leaves the governing axis
# at 153.9 and detection at 16.3x. Sign control on the axis above -- if the
# response tracked |I_y/I_z - 1| rather than the weak axis, this would also fall.
id=aniso_reversed_I_y_over_I_z  section=circular_tube,D=0.600,t=0.01200 stations=32.0 orient=skew extra=I_y_over_I_z=100.0 runs_in_suite=no expect=hold

# --- eleventh round: extra= as a SEQUENCE, in shapes the twelve new tests omit -
# The five accepted shapes in MULTI_EXTRAS are (roll), (roll, aniso), (onode),
# (onode, roll), (none). These two are the pairs it does not cover.
# onode + aniso, no roll: det 4.6044e-12, clean 0.018x.
id=onode_and_aniso_together     section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew extra=orientation_node=0.13,-0.91,0.39,I_y_over_I_z=0.20 runs_in_suite=no expect=hold
# ALL THREE keys at once, on a free direction. Clean out-of-balance 2.2595e-16 =
# 1.018 eps, which is the largest in this corpus and the first above one eps --
# the plan's "0.13-0.49 eps across every configuration" (F2.md D7 item 7) is 7.8x
# below it. Still 0.045x of the ceiling; det 1.0717e-11.
id=all_three_extras_free_dir    section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=0.41,-0.77,0.49 extra=orientation_node=0,0,1,roll=0.61,I_y_over_I_z=0.35 runs_in_suite=no expect=hold
# Two lines the sequence parser must still REFUSE rather than resolve: a key
# repeated across a vector value, and a vector truncated so the next key is
# swallowed as its third component.
id=extra_key_repeated_with_onode section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew extra=roll=0.2,orientation_node=1,0,0,roll=0.4 runs_in_suite=no expect=raise
id=extra_onode_truncated        section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew extra=orientation_node=1,0,I_y_over_I_z=0.5 runs_in_suite=no expect=raise

# --- twelfth round: the two claims are stated over a SPACE and measured on ----
# --- this LIST, and each is refuted by a configuration inside the same space --
# Measured by the reviewer at 121a35a on the shipped runner's own quantity.
#
# CLAIM 1 ("formulation defects, every entry, no exception, no domain") has an
# edge, and the edge is 1.4% away in its own axis from the corpus entry that
# already binds it. Solved rather than sampled: both structural defects fall as
# (L/r_min)^-1.99 -- measured on this family, 900 -> 2885 -- so
# `wrong_dof_index` crosses PATCH_TEST_EXACTNESS_COUNTER at L/r_min ~ 912.
#
#   L/r_min    900     930     1058    1250    1443    1924    2885
#   x counter  1.028   0.964   0.747   0.536   0.403   0.227   0.101
#              ^ lam900_axis_undetectable, the corpus's binding entry
#
# The same family as lam900_axis_undetectable, 3.4% longer. Nothing about it is
# new except the length. clean 8.7042e-17 = 0.0174x the ceiling.
id=lam930_axis_counter_edge     section=circular_tube,D=0.600,t=0.01200 stations=193.4 orient=axis runs_in_suite=no expect=hold
# Unambiguously past the edge: wrong_dof_index 0.747x, dropped_flip 2.930x.
id=lam1058_axis_counter_red     section=circular_tube,D=0.600,t=0.01200 stations=220.0 orient=axis runs_in_suite=no expect=hold
# The edge is NOT one number in L/r_min: on a nearly solid wall (D/t = 2.07) with
# a weak axis, the binding defect is the OTHER one -- dropped_flip 0.736x while
# wrong_dof_index is 15.0x. A single scalar bound would have to cover both.
id=flip_counter_edge_thickwall  section=circular_tube,D=0.600,t=0.29000 stations=35.0 orient=skew extra=I_y_over_I_z=0.01 runs_in_suite=no expect=hold
#
# CLAIM 2's band is saturated by ORDINARY members, and its stated 1.39x of margin
# is a property of the 63 entries it was fitted on. Over 360 random admissible
# configurations (D 0.1-300 m, D/t 3-30, L/r_min 30-200, free direction, free
# roll, I_y/I_z 0.01-0.2) the worst deviation from the recorded curve is 0.594
# against a band of 0.60 -- 0.99 of it. This line crosses.
# ratio 2.5847, deviation 0.6131 = 1.022x the band. L/D 8.3, L/r_min 92.9, and
# the ceiling holds here at 0.053x -- the element is fine, the CURVE is not.
id=band_edge_thickwall_aniso    section=circular_tube,D=2.88803,t=0.547075 stations=24.1118 orient=-0.603839,0.545380,0.581326 extra=roll=-2.399378,I_y_over_I_z=0.0932001 runs_in_suite=no expect=hold
# A second shape from the same region, just inside: ratio 2.4614 = 0.990 of the
# band. Two lines 4% apart on either side of the edge, so the boundary is
# bracketed rather than asserted from one side.
id=band_edge_thickwall_free_dir section=circular_tube,D=4.2183,t=0.91077 stations=23.8329 orient=-0.605654,0.585875,0.538455 extra=roll=-1.667200,I_y_over_I_z=0.093016 runs_in_suite=no expect=hold
# ANISOTROPY IS NOT REQUIRED. A plain circular tube the shipped model can build
# today -- D/t 4.5, L/D 111, a bracing member -- sits at ratio 2.3505, 0.958 of
# the band. clean 4.4363e-16 = 2.00 eps, the largest in this corpus.
id=band_edge_isotropic_bracing  section=circular_tube,D=0.290954,t=0.0640574 stations=32.3916 orient=-0.581897,-0.576984,-0.573136 extra=roll=-0.677087 runs_in_suite=no expect=hold
#
# THE SYNTHETIC ANISOTROPY ROUTE VALIDATES NOTHING, which is a different subject
# from the two claims and is recorded here rather than argued. `_extras` checks
# that the value is finite and stops; `I_y_over_I_z=0` builds a member with ZERO
# bending stiffness in one plane and `=-1.0` builds one with NEGATIVE bending
# stiffness, both silently, both measured at 121a35a. A mechanism assembled
# without complaint is the wrong-answer-that-looks-right this repository exists
# to prevent, and the refusal belongs where the value is parsed.
id=aniso_zero_I_y_must_refuse   section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew extra=I_y_over_I_z=0 runs_in_suite=no expect=raise
id=aniso_negative_I_y_must_refuse section=circular_tube,D=0.600,t=0.01200 stations=9.67 orient=skew extra=I_y_over_I_z=-1.0 runs_in_suite=no expect=raise
