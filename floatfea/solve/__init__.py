"""Assembly, constraints, inertia relief, linear solve (F2/F4).

Inertia relief uses Lagrange multipliers on six rigid-body constraints, so the
multipliers ARE the residual reactions and double as a diagnostic.
"""
